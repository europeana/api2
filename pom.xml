<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.europeana</groupId>
        <artifactId>europeana-parent-pom</artifactId>
        <version>2.4</version>
    </parent>

    <artifactId>api2</artifactId>
    <version>2.17.8-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Search and Record API</name>
    <url>http://www.europeana.eu</url>

    <modules>
        <module>api2-model</module>
        <module>query-syntax</module>
        <module>api2-war</module>
    </modules>

    <scm>
        <connection>scm:git:git@github.com:europeana/api2.git</connection>
        <developerConnection>scm:git:git@github.com:europeana/api2.git</developerConnection>
        <url>https://github.com/europeana/api2</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <corelib.version>2.17.8-SNAPSHOT</corelib.version>
        <!-- corelib now uses metis-common and metis-schema version-->
        <metis.version>18-SNAPSHOT</metis.version>
        <apicommons.version>0.4.13-SNAPSHOT</apicommons.version>
        <translation.api.client.version>0.1.3</translation.api.client.version>

        <!-- dependencies below are also used in Corelib, would be best to match version -->
        <commonsLang3.version>3.17.0</commonsLang3.version>
        <httpclient.version>4.5.14</httpclient.version>
        <jackson.version>2.18.0</jackson.version>
        <jodatime.version>2.13.0</jodatime.version>
        <log4j2.version>2.24.1</log4j2.version>
        <!-- Also note that if we upgrade SpringBoot to v1.4.x or later we break the current info endpoint functionality -->
        <springBoot.version>1.3.8.RELEASE</springBoot.version>
        <springCore.version>6.2.18</springCore.version>
        <springSecurity.version>5.8.16</springSecurity.version>

        <junit.version>4.13.2</junit.version>
        <mockito.version>3.12.4</mockito.version>
        <servletJstl.version>1.2</servletJstl.version>
        <springWebService.version>2.4.7.RELEASE</springWebService.version>
    </properties>

    <repositories>
        <!--Release repositories-->
        <repository>
            <id>libs-release</id>
            <name>libs-release</name>
            <url>https://artifactory-prod.eanadev.org/artifactory/libs-release</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>libs-snapshot</id>
            <name>libs-snapshot</name>
            <url>https://artifactory-prod.eanadev.org/artifactory/libs-snapshot-local</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <!-- Safe 5.3.x Spring Web patch (Fixes Critical CVEs, keeps javax.*) -->
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-web</artifactId>
                <version>${springCore.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-webmvc</artifactId>
                <version>${springCore.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-aop</artifactId>
                <version>${springCore.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-core</artifactId>
                <version>${springCore.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.springframework</groupId>
                        <artifactId>spring-web</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-beans</artifactId>
                <version>${springCore.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-context</artifactId>
                <version>${springCore.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-context-support</artifactId>
                <version>${springCore.version}</version>
            </dependency>


            <!-- Safe 5.8.x Spring Security patch (Fixes Critical CVEs, keeps javax.*) -->
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-core</artifactId>
                <version>${springSecurity.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-web</artifactId>
                <version>${springSecurity.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-config</artifactId>
                <version>${springSecurity.version}</version>
            </dependency>

            <!-- ZooKeeper Upgrade -->
            <dependency>
                <groupId>org.apache.zookeeper</groupId>
                <artifactId>zookeeper</artifactId>
                <version>3.7.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>


    <profiles>
        <profile>
            <id>depcheck</id>
            <build>
                <!-- TO Fix error No plugin found for prefix 'sonar' in the current project and in the plugin groups [org.apache.maven.plugins, org.codehaus.mojo]
                 available from the repositories -->
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.sonarsource.scanner.maven</groupId>
                            <artifactId>sonar-maven-plugin</artifactId>
                            <version>3.7.0.1746</version>
                        </plugin>
                    </plugins>
                </pluginManagement>
                <plugins>
                    <!-- This will be active when running the verify or install goal
                         To skip (e.g. during debugging) use 'mvn clean package')-->
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>10.0.3</version>
                        <configuration>
                            <nvdApiKeyEnvironmentVariable>NVD_APIKEY</nvdApiKeyEnvironmentVariable>
                            <!-- see EA-3505 why we host the known exploited vulnerabilties file ourselves -->
                            <knownExploitedUrl>
                                https://artifactory-prod.eanadev.org/artifactory/ext-release-local/gov/cisa/www/known_exploited_vulnerabilities.json
                            </knownExploitedUrl>
                            <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
                            <failBuildOnCVSS>9</failBuildOnCVSS>
                            <suppressionFiles>
                                <suppressionFile>owasp-suppress.xml</suppressionFile>
                            </suppressionFiles>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
